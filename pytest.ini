[pytest]
markers =
    integration: tests die echte externe services aanroepen (YouTube, Whisper, Ollama). Vereist netwerk en een lokaal draaiende Ollama-server. Draai met: pytest -m integration
testpaths = tests
addopts = -m "not integration"
